% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/downloading.R
\name{download_sceneMetadata}
\alias{download_sceneMetadata}
\title{Download Landsat scene-level metadata from EDI}
\usage{
download_sceneMetadata(
  download_folder,
  product = c("siteSR", "lakeSR"),
  version = "newest",
  ask = TRUE
)
}
\arguments{
\item{download_folder}{A string containing the path to the folder where the
dataset should be saved.}

\item{product}{Either "siteSR" or "lakeSR" - which EDI package to pull
from. The scene-level metadata content is identical between the two, so
this only matters if you want the file's provenance/citation tied to a
particular package's revision lineage.}

\item{version}{Either "newest" or an integer corresponding to the data
package version to use.}

\item{ask}{Logical. Should the user be asked before downloading and
overwriting scene metadata files that already exist locally?}
}
\value{
A named character vector containing the local file paths for the
downloaded scene metadata datasets. Returned invisibly.
}
\description{
A function to facilitate downloading of Landsat scene-level metadata that
accompanies the siteSR and lakeSR data products on the Environmental Data
Initiative (EDI). Scene-level metadata captures per-overpass attributes
(e.g. cloud cover, image quality, geometric accuracy) that aren't carried
through into siteSR/lakeSR's per-site aggregation.
}
\details{
Downloads two components of the scene-level metadata (identical content in
both the \href{https://portal.edirepository.org/nis/mapbrowse?packageid=edi.2254.1}{siteSR}
and \href{https://portal.edirepository.org/nis/mapbrowse?packageid=edi.2114.1}{lakeSR}
EDI packages):
\itemize{
\item reduced column scene-level metadata for Landsat 4, 5, and 7 (.csv)
\item reduced column scene-level metadata for Landsat 8 and 9 (.csv)
}

The two files have slightly different schemas: the Landsat 4/5/7 file has
a single \code{IMAGE_QUALITY} column and \verb{GEOMETRIC_RMSE_VERIFY_QUAD_*}
columns, while the Landsat 8/9 file splits \code{IMAGE_QUALITY} into
\code{IMAGE_QUALITY_OLI} and \code{IMAGE_QUALITY_TIRS} and has no quad columns.
Both files share a \code{sat_id} column (derived from Earth Engine's
\code{system:index}) that can be used to join scene-level metadata back onto
siteSR/lakeSR rows, which carry the same \code{sat_id}.
}
\note{
The downloaded files are large (tens of MB each), so make sure you have
appropriate available storage.
}
\examples{
\dontrun{
download_sceneMetadata(download_folder = "~/Downloads/", product = "siteSR")
}
}
