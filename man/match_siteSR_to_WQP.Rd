% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/building_joining.R
\name{match_siteSR_to_WQP}
\alias{match_siteSR_to_WQP}
\title{Join AquaMatch WQP dataset to siteSR}
\usage{
match_siteSR_to_WQP(
  wqp_path,
  siteSR_path,
  site_list_path,
  save_location,
  time_window = "5 days"
)
}
\arguments{
\item{wqp_path}{Path to the file (.csv or .feather) storing the AquaMatch
parameter data to be joined. Should be data from a single parameter (e.g.,
chlorophyll \emph{a}), as would be saved after using the \code{download_parameters()}
function.}

\item{siteSR_path}{Path to the file (.feather) storing the stacked version of
the siteSR dataset (either DSWE1 or DSWE1a). This is the equivalent of the
direct output of the \code{build_sr()} function when run with siteSR data.}

\item{site_list_path}{Path to the file (.csv) storing the site list for siteSR.
This is included in downloads done using \code{download_siteSR()}.}

\item{save_location}{String. The path where a parquet file containing the output
should be saved. If the string does not end in ".parquet" then an error will occur.}

\item{time_window}{A string indicating the amount of time on either side of the
in-situ measurements that should be used to match to siteSR overpass times, for
example: "2 days", "72 hours". Defaults to "5 days".}
}
\value{
The path to the joined dataset. (Invisible)
}
\description{
A function that creates a join between a specified AquaMatch harmonized Water
Quality Portal (WQP) dataset and the siteSR data product.
}
\details{
Matches a single AquaMatch harmonized WQP product to the siteSR product. Options
for the AquaMatch component are \href{https://portal.edirepository.org/nis/mapbrowse?scope=edi&identifier=1756&revision=2}{chlorophyll \emph{a}}
("chla"), \href{https://portal.edirepository.org/nis/mapbrowse?packageid=edi.1809.1}{dissolved organic carbon}
("doc"), \href{https://portal.edirepository.org/nis/mapbrowse?scope=edi&identifier=2048&revision=2}{Secchi disk depth}
("sdd"), or \href{https://portal.edirepository.org/nis/mapbrowse?packageid=edi.2048.2}{total suspended solids}
("tss").

Note that this function requires a .parquet file for output of the join. This
is because all join computation takes place out of memory and therefore cannot
be completed using other filetypes such as .csv or .feather.
}
\examples{
\dontrun{

}
}
