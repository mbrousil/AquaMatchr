% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/building_joining.R
\name{apply_handoffs}
\alias{apply_handoffs}
\title{Apply intermission handoffs to surface reflectance data}
\usage{
apply_handoffs(
  input_path,
  handoff_path,
  correction_method,
  sat_target,
  algal_mask,
  save_location
)
}
\arguments{
\item{input_path}{Character. Path to the input .parquet dataset containing surface reflectance data.}

\item{handoff_path}{Character. Path to the .csv file containing the handoff coefficients.}

\item{correction_method}{Character. The mathematical correction method to apply.
Valid options are \code{"Roy_deming"}, \code{"Roy_lm"}, or \code{"Gardner_poly"}. These correspond
to Roy method with Deming regression, Roy with ordinary least squares regression,
or Gardner method with quadratic relationship.}

\item{sat_target}{Character. The target satellite mission to harmonize the data to.
Valid options are \code{"LS7"} or \code{"LS8"}.}

\item{algal_mask}{Logical. If TRUE, the algal mask version of the handoff (DSWE1a)
will used. Otherwise DSWE1 (i.e., FALSE).}

\item{save_location}{Character. The destination file path for the corrected .parquet file.
Must end in \code{".parquet"}.}
}
\value{
Invisibly returns the \code{save_location} character string.
}
\description{
Applies satellite handoff coefficients (using either Roy or Gardner methods) to
surface reflectance data. This function standardizes band measurements across
Landsat missions to a target satellite's baseline. It is intended for use on
lakeSR, siteSR, matchups of siteSR with \emph{in-situ} data (e.g., outputs from
\code{match_siteSR_to_WQP()}).

It uses the \code{{arrow}} package to process the data out-of-core and saves the
corrected dataset as a .parquet file. New columns are added
}
\examples{
\dontrun{

}
}
